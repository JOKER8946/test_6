# FWC Module 5 · Test-6 — pinned for a fresh venv on Python 3.11 (CPU only).
# These versions are pinned together on purpose: gensim 4.3.3 requires numpy<2
# and scipy<1.14, and TensorFlow 2.16 is happy on the same numpy. Loosening any
# one of them tends to break the other two.

numpy==1.26.4
scipy==1.13.1
pandas==2.2.2
scikit-learn==1.5.1
joblib==1.4.2

# reading the Kaggle .xlsx files
openpyxl==3.1.5

# recurrent rungs
tensorflow==2.16.2

# GloVe download + loading
gensim==4.3.3

# transfer-learning rung
torch==2.3.1
transformers==4.44.2
accelerate==0.33.0

# figures, tables, dashboard
matplotlib==3.9.2
tabulate==0.9.0
streamlit==1.38.0
altair==5.4.1
